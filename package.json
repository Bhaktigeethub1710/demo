{
    "name": "nyayasetu-root",
    "version": "1.0.0",
    "description": "Root definition for NyayaSetu deployment",
    "scripts": {
        "start": "npm start --prefix backend",
        "build": "npm install && npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}